# Cloudflare Workers 静态资产部署配置
# Workers Builds（Git 集成）部署命令: npx wrangler deploy
# 构建命令（Dashboard 配置）: npm run build
# _redirects 与 _headers 在 Workers 静态资产中原生支持，随 dist/ 自动生效
name = "aerogela"
compatibility_date = "2026-08-27"

[assets]
directory = "./dist"
not_found_handling = "404-page"