# 凭据 — 绝不入库
.credentials/id_ed25519
.credentials/cloudflare-token.txt
**/id_ed25519
**/cloudflare-token.txt

# 浏览器二进制(>100MB,GitHub 拒绝或被克隆拖慢),按 .tools/README.md 重新下载
.tools/browser/chrome-linux64/
# 浏览器系统库解包目录(几十 MB,由 debs/ 内 .deb 重新解包即可恢复,不入库)
.tools/browser/browser-libs/
deliverables.html

# 浏览器持久化 profile(含登录 cookie / localStorage,敏感!绝不入库)
.browser-profiles/
.browser-profiles.tar.gz

# 工作截图留档(含登录流程页面,不入库)
.screenshots/

# 构建产物 / 依赖 / 本地缓存
node_modules/
dist/
.astro/
.package/
.wrangler/
*.log
.DS_Store
# CloakBrowser Chromium 缓存(697M,本地化防重置重下载,不入库)
.tools/browser/cloakbrowser-cache/
# SEO 监控报告与状态基线(每日定时生成,不入库)
.tools/browser/reports/
